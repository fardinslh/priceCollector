version: '3.8'

services:
  shopping-bot:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: shopping-bot
    restart: unless-stopped
    env_file:
      - .env
    environment:
      - NODE_ENV=production
